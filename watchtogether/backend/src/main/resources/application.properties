server.port=8080

# WebSocket
spring.websocket.path=/ws

# Logging
logging.level.com.watchtogether=DEBUG
logging.level.org.springframework.web.socket=INFO

# ── URLs — single source of truth ─────────────────────────────────────────────
# Override these in application-prod.properties or via environment variables.
# On Railway set: SPRING_PROFILES_ACTIVE=prod
watchtogether.app-base=http://localhost:5173
watchtogether.cors.origins=http://localhost:5173,chrome-extension://*
