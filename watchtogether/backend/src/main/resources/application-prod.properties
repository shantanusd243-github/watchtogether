# ── Production overrides (Railway) ────────────────────────────────────────────
# Activated when SPRING_PROFILES_ACTIVE=prod is set in Railway environment vars.

server.port=${PORT:8080}

watchtogether.app-base=https://watchtogether-zeta.vercel.app
watchtogether.cors.origins=https://watchtogether-zeta.vercel.app,chrome-extension://*

logging.level.com.watchtogether=INFO
