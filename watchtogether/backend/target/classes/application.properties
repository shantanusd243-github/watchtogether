server.port=8080

# WebSocket config
spring.websocket.path=/ws

# Logging
logging.level.com.watchtogether=DEBUG
logging.level.org.springframework.web.socket=INFO

# CORS — update for production
watchtogether.cors.origins=http://localhost:5173,chrome-extension://*
